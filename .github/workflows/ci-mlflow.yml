name: CI-MLflow-Training-and-Docker

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  train-and-dockerize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install mlflow pandas scikit-learn joblib

      - name: Run MLflow Project (Training)
        run: |
          mlflow run MLProject

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image using MLflow
        run: |
          mlflow models build-docker \
            --model-uri runs:/$(mlflow runs list --experiment-id 0 --max-results 1 | awk 'NR==2{print $1}') \
            --name saabrimr/gizibalita-logreg

      - name: Push Docker image to Docker Hub
        run: |
          docker push saabrimr/gizibalita-logreg
